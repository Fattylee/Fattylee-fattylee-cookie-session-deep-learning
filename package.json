{
  "name": "fattylee-cookie-session",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src -d code -w ",
    "start:dev": "nodemon code -w code",
    "start:all": "concurrently 'yarn start:dev' 'yarn build'",
    "prestart": "babel src -d code",
    "start": "node code"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/cli": "^7.10.1",
    "@babel/core": "^7.10.2",
    "@babel/preset-env": "^7.10.2",
    "concurrently": "^5.2.0"
  }
}
